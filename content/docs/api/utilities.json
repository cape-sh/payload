{
  "swagger": "2.0",
  "info": {
    "title": "Sample API",
    "description": "API description in Markdown.",
    "version": "1.0.0"
  },
  "host": "api.example.com",
  "schemes": [
    "https"
  ],
  "paths": {
    "/Utility/sshtest": {
      "post": {
        "description": "Performs a connectivity test from caepe to a remote ssh server",
        "produces": [
          "application/json"
        ],
        "parameters": [
          {
            "name": "imageregistry_post",
            "in": "body",
            "schema": {
              "$ref": "#/definitions/sshTest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "schema": {
              "$ref": "#/definitions/sshTestResponse"
            }
          },
          "400": {
            "description": "error",
            "schema": {
              "$ref": "#/definitions/errors"
            }
          },
          "401": {
            "description": "Unauthorized"
          },"422": {
            "description": "Unprocessable Entity",
            "schema": {
              "$ref": "#/definitions/error"
            }
          }
        }
      }
    }
  },
  "definitions": {
    "sshTest" : {
      "properties": {
        "url": {
          "type": "string"
        },
        "authType": {
          "type": "string",
          "enum": [
            "key",
            "password"
          ]
        },
        "userName": {
          "type": "string"
        },
        "password": {
          "type": "string"
        },
        "key": {
          "type": "string"
        }
      }
    },
    "sshTestResponse": {
      "properties": {
        "url": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "enum": [
            "Success",
            "Failed"
          ]
        }
      }
    },
    "error": {
      "properties": {
        "code": {
          "type": "integer"
        },
        "reason": {
          "type": "string"
        },
        "message": {
          "type": "string"
        },
        "status": {
          "type": "string"
        }
      },
      "required": [
        "code",
        "message"
      ]
    },
    "errors": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/error"
      }
    }
  },
  "securityDefinitions": {
    "bearerAuth": {
      "name": "Authorization",
      "in": "header",
      "type": "apiKey",
      "description": "JWT Authorization header"
    }
  },
  "security": [
    {
      "bearerAuth": []
    }
  ]
}
